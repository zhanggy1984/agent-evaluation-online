# obs-sdk（占位）

> T-0.3 建目录骨架；真内容随 **T-1.5**（SDK 雏形：logging handler + 事件打点 + 内存队列批量 → kafka-python，§2 样例三份直接可单测）与 detail §11.1（对外 API v1）落地。
> 技术：`kafka-python` + 标准库 + structlog 可选集成（detail §1.1）；目录规划 `sdk/pyproject.toml + obs_sdk/`（detail §1.2）。
